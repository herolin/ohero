# CLAUDE.md

本文件供 AI 助理(以及開發者)理解本專案的目標、架構與開發慣例。

> **目前狀態(2026-07):階段 4 完成(連線基礎)。** 單機完整可玩(RWD + i18n),對戰連線層已就緒:
> `multiplayer/protocol.ts`(同步訊息型別)、`multiplayer/room.ts`(房間 ID / `?room=` 分享連結,
> 有單元測試)、`multiplayer/connection.ts`(PeerJS 建房/加入封裝)、`ui/lobby.ts`(建房/選模式/
> 分享連結/等待對手/斷線處理),並由 `main.ts` 路由(單機 ⇄ 大廳)。共 24 個 Vitest 測試通過,
> Playwright 實測大廳流程正常。**實際對戰玩法(競速/共用盤)尚未實作**(見第 9 節路線圖階段 5–6)。
> 註:PeerJS 需連線公用 signaling 伺服器,真正 P2P 握手須於一般網路 + 雙裝置實測(階段 7)。

---

## 1. 專案概觀

**game1-bomb** 是一款仿 Windows 經典「踩地雷(Minesweeper)」的網頁遊戲,並支援**即時多人對戰**。

核心特性:
- **純前端**:無自建後端伺服器,可部署為靜態網站(如 GitHub Pages)。
- **RWD 響應式設計**:桌機、平板、手機皆可遊玩(滑鼠與觸控雙支援)。
- **即時對戰**:透過 **WebRTC P2P(PeerJS)** 讓兩位玩家經由分享連結對戰,無需自建/維運伺服器。
- **多國語系(i18n)**:UI 支援多語言,**預設英文 (en)**;首波提供 **en / zh-TW(繁中)/ zh-CN(簡中)**,並可持續擴充。

---

## 2. 技術棧(預定)

| 項目 | 選用 | 說明 |
|------|------|------|
| 語言 | TypeScript | 型別安全、邏輯清晰 |
| 建置工具 | Vite | 開發熱更新、輸出純靜態檔 |
| UI 渲染 | HTML + CSS Grid + DOM | 格線用 CSS Grid,不使用 Canvas |
| 樣式 | 原生 CSS(CSS 變數 + Media Query) | 精準控制 RWD,無額外依賴 |
| 即時連線 | PeerJS(WebRTC 封裝) | 瀏覽器對瀏覽器直連 |
| 可重現亂數 | seedrandom 或自寫 PRNG | 同 seed 產生同一張雷圖,確保對戰公平 |
| 測試 | Vitest | 與 Vite 同生態,測核心邏輯 |
| 部署 | GitHub Pages / 任意靜態主機 | 前端純靜態;連線走 PeerJS |

---

## 3. 預定目錄結構

```
game1-bomb/
├── index.html                # 進入點
├── src/
│   ├── main.ts               # 初始化、事件綁定
│   ├── game/                 # 純遊戲邏輯(與 UI/網路解耦)
│   │   ├── board.ts          # 佈雷、鄰數計算、自動展開
│   │   ├── gameState.ts      # 狀態機:單機/競速/合作/搶解鎖
│   │   ├── rng.ts            # 可重現亂數(seed) — 對戰同盤前置
│   │   └── types.ts          # 型別:Cell、Difficulty、Mode、Ownership 等
│   ├── multiplayer/          # 對戰模組
│   │   ├── connection.ts     # PeerJS 建房/加入房、收送訊息
│   │   ├── protocol.ts       # 同步訊息型別定義
│   │   ├── room.ts           # 房間 ID、分享連結(?room=XXXX)解析
│   │   └── authority.ts      # Host 權威:意圖排序、盤面 delta、格子歸屬
│   ├── ui/
│   │   ├── render.ts         # 棋盤 + 狀態列 + 進度/比分 + 歸屬著色
│   │   ├── input.ts          # 滑鼠(左右鍵)/ 觸控(點擊/長按)
│   │   └── lobby.ts          # 建房/加入/選模式/等待對手畫面
│   ├── i18n/                 # 多國語系
│   │   ├── index.ts          # i18n 核心:語言偵測、切換、t() 取字串
│   │   └── locales/
│   │       ├── en.ts         # 英文(預設 / fallback)
│   │       ├── zh-TW.ts      # 繁體中文
│   │       └── zh-CN.ts      # 簡體中文
│   └── styles/
│       └── main.css          # 版面 + RWD + 主題變數
├── tests/
│   ├── board.test.ts         # 核心邏輯單元測試
│   └── rng.test.ts           # 驗證同 seed 產生同一張盤
├── package.json
├── tsconfig.json
├── vite.config.ts
└── CLAUDE.md
```

**分層原則**:`game/` 為純邏輯(可單元測試、不依賴 DOM 或網路);`ui/` 負責渲染與輸入;
`multiplayer/` 負責連線與同步。UI 與網路都依賴 `game/`,但 `game/` 不反向依賴它們。

---

## 4. 遊戲規則

### 4.1 單機基本玩法
- 三種難度:初級 9×9(10 雷)、中級 16×16(40 雷)、高級 16×30(99 雷)。
- 左鍵翻開格子;**首次點擊保證不踩雷**(第一擊之後才佈雷)。
- 翻到空白格(周圍 0 雷)時,自動遞迴展開相鄰區域。
- 右鍵 / 長按插旗(手機以長按對應右鍵)。
- 數字格顯示周圍地雷數(1–8 經典配色)。
- 勝:翻開所有非雷格;敗:點到雷。
- 狀態列:剩餘地雷數、計時器、重新開始鈕。

### 4.2 對戰模式(共通前提:**踩雷即輸**)

| 模式 | 盤面 | 勝負判定 |
|------|------|----------|
| **競速 Race** | 各自獨立、同 seed 同圖 | 先清完所有安全格者勝;先踩雷者敗、對方勝 |
| **合作 Co-op** | 共用同一盤 | 任一人踩雷 → 兩人共同敗;合力清完 → 共同勝 |
| **搶解鎖 Claim** | 共用同一盤 | 見下方規則 |

**搶解鎖(Claim)結束判定**(已定案):
- 有人踩雷 → **該人立刻輸、對方直接贏**(不比佔格數)。
- 只有在「無人踩雷、全盤清完」時,才比**翻開較多格**者勝。
- **平手(佔格數相同)→ 判和局。**
- 每一格歸屬於「先翻開者」,UI 以不同顏色標示歸屬。

**對戰盤面難度**:由建房方(Host)選擇(初/中/高),**預設中級 16×16、40 雷**。
**首擊安全**:以整局「第一次翻格」那一擊為準才佈雷;共用盤模式由 Host 處理。

---

## 5. 對戰架構(WebRTC / PeerJS)

因採 **P2P 無伺服器權威**,依模式分兩種同步策略:

### 5.1 獨立盤(競速)
兩人各持一份盤,互不影響。只需同步:開局 seed、各自進度、結束事件。頻寬低、無衝突。

### 5.2 共用盤(合作 / 搶解鎖)— Host 權威模型
- **建房方(Host)持有唯一真實盤面。**
- 雙方點擊皆轉為「意圖(intent)」送給 Host。
- Host 決定順序、套用結果、標記格子歸屬,再把**盤面變化(delta)廣播**給雙方。
- 訪客端(Guest)只做顯示與送意圖,不自行改盤,以避免兩份盤面分歧與同時點擊衝突。

### 5.3 連線流程
1. Host 按「建立對戰」→ 產生房間 ID → 組成分享連結(`?room=XXXX`)。
2. 把連結傳給對方;對方開啟連結自動加入該房。
3. 雙方連上後,由 Host 指定模式、產生 seed(或初始盤)並同步。
4. 同步倒數 → 同時開局 → 過程即時互傳 → 判定勝負 → 可再來一局。

### 5.4 同步協定(`protocol.ts` 預定訊息)
共通:`join`、`mode`、`start`、`rematch`、`disconnect`
- **競速**:`progress`(自己已翻格數,節流 200–500ms)、`gameover`
- **合作 / 搶解鎖**:`intent`(想翻開/插旗 (x,y))、`reveal`(Host 廣播:翻開哪些格、數字、歸屬、是否踩雷)、`score`(搶解鎖比分)、`gameover`

### 5.5 已知限制
- 本設計為**客戶端信任**,適合休閒對戰,不做嚴格防作弊。
- WebRTC 在嚴格 NAT 環境下可能需要 TURN 才能連線;初版僅用公用 STUN。
- 斷線時顯示提示、該局結束,不做重連續盤。

---

## 6. 開發工作流程(預定指令)

> 專案已初始化,以下指令即可使用(`build` 會先跑 `tsc --noEmit` 型別檢查)。

```bash
npm install        # 安裝相依套件
npm run dev        # 本地開發伺服器(Vite,熱更新)
npm run build      # 產生靜態檔至 dist/
npm run preview    # 預覽 build 結果
npm test           # 執行 Vitest 單元測試
```

**本地對戰測試**:開兩個瀏覽器分頁(或兩台裝置),一方建房、複製 `?room=XXXX` 連結給另一方加入。

---

## 7. 開發慣例

- **語言/型別**:TypeScript,啟用嚴格模式;公開介面標註型別。
- **分層解耦**:遊戲邏輯放 `game/` 且為純函式化、不觸碰 DOM/網路,以利測試。
- **命名**:檔案 kebab/camel 一致;型別用 PascalCase;函式/變數用 camelCase。
- **亂數**:對戰相關的佈雷一律走 `rng.ts`(seed 可重現);嚴禁在對戰邏輯直接使用 `Math.random()`,否則雙方盤面會分歧。
- **測試**:核心邏輯(佈雷、展開、勝負、同 seed 同盤)須有 Vitest 測試。
- **UI 文案 / i18n**:所有面向使用者的字串一律走 i18n `t('key')`,**禁止在 UI 硬編寫死文字**;字串定義集中於 `src/i18n/locales/`。**預設語言英文 (en)**,缺字回退至 en。新增文案時三個語系(en / zh-TW / zh-CN)須同步補齊。語言選擇可存 `localStorage`,並以瀏覽器 `navigator.language` 作初次偵測。

---

## 8. Git 與分支慣例

- 功能開發分支:`claude/claude-md-docs-22o0b2`(本次文件工作分支)。
- 於指定分支開發並提交,推送使用 `git push -u origin <branch-name>`。
- 提交訊息清楚描述變更;不主動建立 Pull Request(除非明確要求)。
- 未經明確許可,不推送至其他分支。

---

## 9. 實作路線圖(TODO)

建議順序:**單機 → 競速(先驗證連線)→ 共用盤(合作/搶解鎖,難點在 `authority.ts`)**。

**階段 1:初始化** ✅
- [x] 建立 Vite + TS 骨架、安裝 PeerJS
- [x] 撰寫 CLAUDE.md

**階段 2:單機核心 + 可重現亂數** ✅
- [x] 型別/棋盤模型、佈雷、首擊安全、鄰數、自動展開、勝負判定
- [x] `rng.ts`(seed);測試同 seed 產生同盤
- [x] 核心邏輯單元測試(16 個測試通過)

**階段 3:單機介面 + RWD + i18n** ✅
- [x] DOM 渲染、狀態列、滑鼠/觸控、數字配色、Media Query 適配
- [x] i18n 核心與語言切換 UI;建立 en / zh-TW / zh-CN 語系檔(預設 en)

**階段 4:連線基礎** ✅
- [x] PeerJS 建房/加入、分享連結(`?room=XXXX`)、大廳選模式、等待對手、斷線處理

**階段 5:競速模式**
- [ ] 同 seed 開局、倒數起跑、對手進度列、勝負同步、再來一局

**階段 6:共用盤模式(合作 / 搶解鎖)**
- [ ] `authority.ts`:意圖排序、盤面 delta 廣播、格子歸屬
- [ ] 合作:共同勝/敗判定
- [ ] 搶解鎖:即時比分、清盤後比數判定(平手和局)、格子歸屬著色
- [ ] 同時點擊衝突處理

**階段 7:測試與部署**
- [ ] 雙裝置實測三模式、跨瀏覽器
- [ ] GitHub Pages 靜態部署
