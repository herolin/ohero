// Entry point. Stage 1 scaffold — game logic, UI and multiplayer modules
// are implemented in later stages (see CLAUDE.md roadmap).

const app = document.querySelector<HTMLDivElement>('#app');

if (app) {
  app.textContent = 'Minesweeper — coming soon';
}
